<idea-plugin>
    <id>com.olezelelabs.dockerrunner</id>
    <name>Docker Runner</name>
    <vendor url="https://github.com/lukasalmeida">lukasalmeida</vendor>

    <description><![CDATA[
      <h1>Docker Runner</h1>
    <p>Uma ferramenta leve e eficiente para rodar e gerenciar o seu ambiente Docker diretamente da barra de navegação (Navbar) do seu IDE JetBrains com um único clique.</p>

    <h2>Recursos</h2>
    <ul>
        <li><strong>Ação Inteligente (Up/Down):</strong> Detecta o status dos seus containers automaticamente. Clique para disparar <code>docker compose up --build -d</code> ou clique novamente (quando estiver verde) para executar <code>docker compose down</code> e derrubar o ambiente.</li>
        <li><strong>Execução Assíncrona e Feedback:</strong> O processo roda inteiramente em segundo plano, exibindo uma barra de carregamento nativa no rodapé do IDE para que a sua interface nunca congele durante o *build*.</li>
        <li><strong>Compatibilidade Universal:</strong> Funciona perfeitamente em toda a suíte de IDEs da JetBrains (IntelliJ IDEA, WebStorm, GoLand, PyCharm, etc.).</li>
    </ul>

    <h2>Botão já ativo na barra superior</h2>
    <p>Ao instalar o plugin, o botão é exibido automaticamente na barra superior do IDE, sem necessidade de personalizar a toolbar.</p>
    <p><strong>Pronto!</strong> Sempre que o seu projeto contiver um arquivo <code>Dockerfile</code> e um <code>docker-compose.yml</code>, a extensão estará ativa. Basta aguardar o ícone mudar para verde indicando que os seus containers estão rodando com sucesso.</p>

    <div class="footer">
        <h2>Instalação e Suporte</h2>
        <p>Para instalar manualmente, acesse os arquivos de distribuição gerados no projeto.</p>
        <p>Para suporte, dúvidas ou envio de sugestões, visite o <a href="https://github.com/seu-usuario/docker-runner">repositório oficial no GitHub</a>.</p>
    </div>
]]></description>

    <!-- Define a dependência mínima da plataforma -->
    <depends>com.intellij.modules.platform</depends>

    <actions>
        <group id="DockerRunnerToolbarGroup"
               text="Docker Runner"
               compact="true"
               popup="false">
            <action id="DockerComposeCleanAction"
                    class="com.olezelelabs.dockerrunner.DockerComposeCleanAction"
                    text="Clean (Down -v)"
                    description="Executa docker compose down -v com confirmação"
                    icon="/icons/clean.svg"/>

            <action id="DockerComposeRunAction"
                    class="com.olezelelabs.dockerrunner.DockerComposeRunAction"
                    text="Docker Up"
                    description="Roda docker compose up --build -d no projeto atual"
                    icon="/icons/play.svg"/>

            <!-- Exibe o botão automaticamente na barra superior principal do IDE após a instalação -->
            <add-to-group group-id="MainToolBar" anchor="last"/>
            <!-- Mantém o botão também na barra de navegação do projeto para acessibilidade -->
            <add-to-group group-id="NavBarToolBar" anchor="after" relative-to-action="NavBarVcsGroup"/>
        </group>
    </actions>
</idea-plugin>